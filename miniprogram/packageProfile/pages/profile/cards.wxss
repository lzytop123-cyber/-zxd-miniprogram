.list-page {
  min-height: 100vh;
  padding-bottom: 32rpx;
}

.list-card {
  margin-bottom: 24rpx;
}

.list-card .name,
.order .row:first-child {
  font-size: 30rpx;
  font-weight: 700;
  color: var(--color-text-brand);
}

.list-card .info,
.order .sub {
  font-size: 24rpx;
  color: var(--color-text-secondary);
  margin-top: 8rpx;
  line-height: 1.5;
}

.order .row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 8rpx 0;
}

.order .status {
  font-size: 24rpx;
  color: var(--color-primary);
  font-weight: 600;
}

/* 优惠券 */
.coupon-item {
  position: relative;
  border-left: 8rpx solid var(--color-primary);
  margin-bottom: 24rpx;
}

.coupon-item .name {
  font-size: 32rpx;
  font-weight: 700;
  color: var(--color-text-brand);
}

.coupon-item .info {
  font-size: 24rpx;
  color: var(--color-text-secondary);
  margin-top: 8rpx;
}

.coupon-item .status {
  position: absolute;
  top: 32rpx;
  right: 32rpx;
  font-size: 22rpx;
  padding: 4rpx 16rpx;
  border-radius: 40rpx;
  background: var(--color-primary-soft);
  color: var(--color-text-brand);
}

/* 钱包 */
.wallet-hero {
  background: linear-gradient(135deg, #52B788 0%, #2D6A4F 100%);
  color: var(--color-bg-card);
  margin: 24rpx;
  border-radius: 32rpx;
  padding: 48rpx 32rpx;
  box-shadow: var(--shadow-brand);
}

.wallet-hero .balance-label {
  font-size: 26rpx;
  opacity: 0.9;
}

.wallet-hero .balance-val {
  font-size: 56rpx;
  font-weight: 700;
  margin-top: 8rpx;
}

.amounts {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  margin: 24rpx 0;
}

.amt {
  flex: 1;
  min-width: 140rpx;
  text-align: center;
  padding: 24rpx 16rpx;
  border-radius: 16rpx;
  border: 2rpx solid var(--color-primary-soft);
  color: var(--color-text-brand);
  font-size: 28rpx;
  font-weight: 600;
}

.amt.active {
  background: var(--color-primary);
  border-color: var(--color-primary);
  color: var(--color-bg-card);
}

.section-head {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--color-text-brand);
  margin-bottom: 16rpx;
}

.action-bar {
  display: flex;
  gap: 16rpx;
  margin-top: 24rpx;
  padding: 24rpx;
}

.action-btn {
  flex: 1;
  text-align: center;
  padding: 20rpx 0;
  border-radius: 16rpx;
  font-size: 28rpx;
  font-weight: 600;
  color: var(--color-text-brand);
  background: var(--green-100);
}

.action-btn.primary {
  background: var(--color-primary);
  color: var(--color-bg-card);
}

.empty-hint {
  margin-top: 12rpx;
  font-size: 24rpx;
  color: var(--color-text-secondary);
}

.log {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20rpx 0;
  border-bottom: 2rpx solid var(--color-bg-page);
}

.log:last-child {
  border-bottom: none;
}

.log-amt,
.pts {
  font-size: 30rpx;
  font-weight: 700;
  color: var(--color-primary);
}

.log .time {
  font-size: 22rpx;
  color: var(--color-text-secondary);
  margin-top: 4rpx;
}

/* 积分 */
.points-hero {
  text-align: center;
  padding: 16rpx 0 32rpx;
  border-bottom: 2rpx solid var(--color-bg-page);
  margin-bottom: 16rpx;
}

.points-hero .total {
  font-size: 56rpx;
  font-weight: 700;
  color: var(--color-primary);
}

.points-hero .total-label {
  font-size: 26rpx;
  color: var(--color-text-secondary);
  margin-top: 8rpx;
}

/* 邀请 */
.code-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: var(--color-bg-page);
  border-radius: 16rpx;
  padding: 24rpx;
  margin: 16rpx 0;
}

.code {
  font-size: 36rpx;
  font-weight: 700;
  color: var(--color-text-brand);
  letter-spacing: 4rpx;
}

.copy-btn {
  padding: 12rpx 28rpx;
  background: var(--color-primary);
  color: var(--color-bg-card);
  border-radius: 40rpx;
  font-size: 24rpx;
}

.input {
  background: var(--color-bg-page);
  border: 2rpx solid var(--color-primary-soft);
  border-radius: 16rpx;
  padding: 24rpx;
  margin: 16rpx 0;
  font-size: 28rpx;
}

/* 人脸 */
.face-status {
  text-align: center;
  padding: 32rpx 0;
}

.face-icon {
  font-size: 80rpx;
  margin-bottom: 16rpx;
}

.face-status .status {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--color-text-brand);
}

.face-status.done .status {
  color: var(--color-primary);
}

/* ============ 我的期限卡页 ============ */
.cards-page {
  min-height: 100vh;
  padding-bottom: 32rpx;
}

.cards-hero {
  margin: 24rpx;
  padding: 36rpx 32rpx 28rpx;
  border-radius: 32rpx;
  background: linear-gradient(135deg, #2D6A4F 0%, #40916C 52%, #52B788 100%);
  box-shadow: var(--shadow-brand);
  color: var(--color-bg-card);
}

.hero-main {
  display: flex;
  align-items: baseline;
  gap: 12rpx;
}

.hero-count {
  font-size: 64rpx;
  font-weight: 800;
  line-height: 1;
}

.hero-label {
  font-size: 28rpx;
  opacity: 0.92;
}

.hero-actions {
  display: flex;
  gap: 16rpx;
  margin-top: 28rpx;
}

.hero-btn {
  flex: 1;
  text-align: center;
  padding: 18rpx 0;
  border-radius: 999rpx;
  font-size: 26rpx;
  font-weight: 600;
  background: rgba(255, 255, 255, 0.18);
  color: var(--color-bg-card);
}

.hero-btn.primary {
  background: var(--color-bg-card);
  color: var(--color-text-brand);
}

.cards-list {
  padding: 0 24rpx;
}

.ticket-card {
  position: relative;
  overflow: hidden;
  margin-bottom: 24rpx;
  padding: 28rpx 32rpx 32rpx;
  border-radius: 28rpx;
  background: linear-gradient(135deg, #2D6A4F 0%, #40916C 58%, #52B788 100%);
  color: var(--color-bg-card);
  box-shadow: var(--shadow-brand);
}

.ticket-deco {
  position: absolute;
  right: 0;
  top: 0;
  bottom: 0;
  width: 140rpx;
  background: repeating-linear-gradient(
    -45deg,
    transparent,
    transparent 10rpx,
    rgba(255, 255, 255, 0.05) 10rpx,
    rgba(255, 255, 255, 0.05) 20rpx
  );
  pointer-events: none;
}

.ticket-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16rpx;
}

.ticket-type {
  font-size: 22rpx;
  padding: 4rpx 16rpx;
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.2);
}

.ticket-badge {
  font-size: 22rpx;
  padding: 4rpx 16rpx;
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.95);
  color: var(--color-text-brand);
  font-weight: 600;
}

.ticket-name {
  font-size: 34rpx;
  font-weight: 700;
  line-height: 1.35;
  padding-right: 80rpx;
}

.ticket-meta {
  margin-top: 12rpx;
  font-size: 24rpx;
  opacity: 0.88;
  line-height: 1.5;
}

.ticket-meta.highlight {
  font-size: 26rpx;
  font-weight: 600;
  opacity: 1;
}

.ticket-rule {
  margin-top: 16rpx;
  padding-top: 16rpx;
  border-top: 2rpx dashed rgba(255, 255, 255, 0.25);
  font-size: 22rpx;
  opacity: 0.82;
  line-height: 1.5;
}

.cards-empty {
  margin: 24rpx;
  padding: 64rpx 32rpx;
  text-align: center;
  background: var(--color-bg-card);
  border-radius: 32rpx;
  box-shadow: var(--shadow-card);
}

.cards-empty .empty-icon {
  font-size: 64rpx;
  margin-bottom: 16rpx;
}

.empty-title {
  font-size: 32rpx;
  font-weight: 700;
  color: var(--color-text-brand);
}

.empty-desc {
  margin-top: 12rpx;
  font-size: 24rpx;
  color: var(--color-text-secondary);
  line-height: 1.6;
}

.empty-actions {
  display: flex;
  gap: 16rpx;
  margin-top: 36rpx;
}

.empty-btn {
  flex: 1;
  padding: 22rpx 0;
  border-radius: 999rpx;
  font-size: 28rpx;
  font-weight: 600;
  background: var(--green-100);
  color: var(--color-text-brand);
}

.empty-btn.primary {
  background: var(--color-primary);
  color: var(--color-bg-card);
}
