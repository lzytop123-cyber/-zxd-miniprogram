.page-report {
  min-height: 100vh;
}

.blocked-tip {
  padding: 120rpx 48rpx;
  text-align: center;
  font-size: 28rpx;
  color: #8C9BA5;
}

/* 报告模式：底部避让悬浮 tabBar */
.report-body {
  padding-bottom: calc(160rpx + env(safe-area-inset-bottom));
}

/* 助手模式：固定高度，聊天区滚动 + 输入框吸底（预留悬浮 tabBar 空间） */
.page-report.chat-mode {
  height: 100vh;
  box-sizing: border-box;
  padding-bottom: calc(140rpx + env(safe-area-inset-bottom));
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

/* ============ 顶部分段切换 ============ */
.seg-wrap {
  flex-shrink: 0;
  padding: 20rpx 24rpx 8rpx;
}

.seg {
  display: flex;
  padding: 6rpx;
  background: #E4EFE8;
  border-radius: 40rpx;
}

.seg-item {
  flex: 1;
  text-align: center;
  padding: 16rpx 0;
  font-size: 26rpx;
  font-weight: 600;
  color: #5C7567;
  border-radius: 36rpx;
  transition: all 0.2s ease;
}

.seg-item.on {
  background: #FFFFFF;
  color: #2D6A4F;
  box-shadow: 0 4rpx 14rpx rgba(28, 43, 32, 0.1);
}

.wb-body { padding-bottom: calc(200rpx + env(safe-area-inset-bottom)); }
.wb-search-bar { display: flex; gap: 12rpx; padding: 8rpx 24rpx 8rpx; }
.wb-search {
  flex: 1;
  height: 72rpx;
  padding: 0 24rpx;
  background: #FFFFFF;
  border-radius: 36rpx;
  font-size: 26rpx;
}
.wb-search-btn {
  width: 120rpx;
  height: 72rpx;
  line-height: 72rpx;
  text-align: center;
  background: #52B788;
  color: #fff;
  border-radius: 36rpx;
  font-size: 26rpx;
  font-weight: 600;
}
.wb-chips { white-space: nowrap; padding: 8rpx 24rpx; }
.wb-chips.wrap { white-space: normal; display: flex; flex-wrap: wrap; gap: 12rpx; }
.wb-chip {
  display: inline-block;
  margin-right: 12rpx;
  padding: 10rpx 24rpx;
  background: #FFFFFF;
  color: #5C7567;
  border-radius: 32rpx;
  font-size: 24rpx;
}
.wb-chip.on { background: #52B788; color: #fff; font-weight: 600; }
.wb-chip.manage { background: #D8F3DC; color: #2D6A4F; }
.wb-list { padding: 8rpx 24rpx 0; }
.wb-card {
  display: flex;
  gap: 20rpx;
  margin-bottom: 16rpx;
  padding: 20rpx;
  background: #FFFFFF;
  border-radius: 24rpx;
  box-shadow: 0 4rpx 20rpx rgba(28, 43, 32, 0.05);
}
.wb-cover {
  width: 160rpx;
  height: 160rpx;
  border-radius: 16rpx;
  background: #E8ECEA;
  flex-shrink: 0;
}
.wb-cover.ph {
  display: flex;
  align-items: center;
  justify-content: center;
  color: #8C9BA5;
  font-size: 22rpx;
}
.wb-card-body { flex: 1; min-width: 0; }
.wb-title {
  font-size: 28rpx;
  color: #1C2B20;
  line-height: 1.45;
  max-height: 80rpx;
  overflow: hidden;
}
.wb-meta {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 12rpx;
  font-size: 22rpx;
  color: #6F7D73;
}
.wb-st { padding: 2rpx 12rpx; border-radius: 8rpx; }
.wb-st.s0 { color: #B45309; background: #FEF3C7; }
.wb-st.s1 { color: #B91C1C; background: #FEE2E2; }
.wb-st.s2 { color: #166534; background: #D8F3DC; }
.wb-empty { padding: 80rpx 48rpx 40rpx; text-align: center; }
.wb-empty-title { font-size: 34rpx; font-weight: 700; color: #2D6A4F; }
.wb-empty-sub { margin-top: 12rpx; font-size: 24rpx; color: #8C9BA5; }
.wb-empty-btn {
  margin: 36rpx auto 0;
  width: 240rpx;
  height: 80rpx;
  line-height: 80rpx;
  background: #52B788;
  color: #fff;
  border-radius: 40rpx;
  font-weight: 600;
}
.wb-tip { text-align: center; color: #8C9BA5; font-size: 24rpx; padding: 16rpx; }
.wb-fab {
  position: fixed;
  right: 40rpx;
  bottom: calc(160rpx + env(safe-area-inset-bottom));
  width: 104rpx;
  height: 104rpx;
  line-height: 96rpx;
  text-align: center;
  font-size: 64rpx;
  color: #fff;
  background: #52B788;
  border-radius: 50%;
  box-shadow: 0 10rpx 28rpx rgba(82, 183, 136, 0.4);
}

.ai-disclaimer {
  flex-shrink: 0;
  margin: 0 24rpx 8rpx;
  padding: 12rpx 20rpx;
  text-align: center;
  font-size: 22rpx;
  font-weight: 600;
  color: #8A6D3B;
  background: #FFF8E8;
  border-radius: 12rpx;
  border: 1rpx solid #F0E0B8;
}

.ai-tag {
  display: inline-block;
  margin: 0 0 8rpx;
  padding: 2rpx 12rpx;
  font-size: 20rpx;
  font-weight: 600;
  color: #2D6A4F;
  background: rgba(45, 106, 79, 0.12);
  border-radius: 8rpx;
  line-height: 1.4;
}

/* ============ 学习报告 ============ */
.report-card {
  margin-bottom: 24rpx;
}

.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 32rpx;
}

.header-title {
  font-size: 36rpx;
  font-weight: 700;
  color: #2D6A4F;
}

.scope {
  font-size: 24rpx;
  color: #52B788;
  padding: 8rpx 20rpx;
  background: #D8F3DC;
  border-radius: 40rpx;
}

.my-rank {
  text-align: center;
  margin-bottom: 32rpx;
  padding: 32rpx;
  background: linear-gradient(135deg, #D8F3DC, #F4FAF6);
  border-radius: 24rpx;
}

.rank-no {
  font-size: 56rpx;
  font-weight: 700;
  color: #52B788;
}

.title-badge {
  display: inline-block;
  background: #52B788;
  color: #FFFFFF;
  padding: 8rpx 24rpx;
  border-radius: 40rpx;
  font-size: 24rpx;
  margin-top: 12rpx;
}

.stat-row {
  display: flex;
  justify-content: space-around;
  margin-bottom: 24rpx;
  padding: 24rpx 0;
  border-top: 2rpx solid #F4FAF6;
  border-bottom: 2rpx solid #F4FAF6;
}

.stat {
  text-align: center;
}

.num {
  font-size: 32rpx;
  font-weight: 700;
  color: #1C2B20;
}

.stat-label {
  font-size: 24rpx;
  color: #8C9BA5;
  margin-top: 8rpx;
}

.beat {
  text-align: center;
  color: #8C9BA5;
  font-size: 24rpx;
  margin-bottom: 32rpx;
}

.tabs {
  display: flex;
  gap: 16rpx;
  margin-bottom: 24rpx;
}

/* ============ 每日学习时长趋势 ============ */
.daily-chart {
  display: flex;
  align-items: flex-end;
  gap: 8rpx;
  height: 200rpx;
  padding: 12rpx 4rpx 0;
  margin-bottom: 28rpx;
  overflow-x: auto;
}

.daily-bar {
  flex: 1 0 auto;
  min-width: 36rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-end;
  height: 100%;
}

.daily-bar-track {
  width: 22rpx;
  height: 150rpx;
  display: flex;
  align-items: flex-end;
  justify-content: center;
}

.daily-bar-fill {
  width: 100%;
  border-radius: 8rpx 8rpx 4rpx 4rpx;
  background: linear-gradient(180deg, #52B788, #2D6A4F);
  transition: height 0.3s ease;
  min-height: 4rpx;
}

.daily-bar-label {
  margin-top: 8rpx;
  font-size: 18rpx;
  color: #9CACA2;
  white-space: nowrap;
}

.daily-empty {
  padding: 36rpx 0;
  text-align: center;
  font-size: 24rpx;
  color: #9CACA2;
  margin-bottom: 12rpx;
}

.section-title.inner {
  padding: 0;
  margin-bottom: 16rpx;
}

.rank-item {
  display: flex;
  padding: 24rpx 16rpx;
  border-bottom: 2rpx solid #F4FAF6;
  align-items: center;
  border-radius: 16rpx;
}

.rank-item.self {
  background: #D8F3DC;
}

.rank {
  width: 64rpx;
  font-weight: 700;
  color: #52B788;
  font-size: 28rpx;
}

.name {
  flex: 1;
  color: #1C2B20;
}

.mins {
  color: #8C9BA5;
  font-size: 24rpx;
}

/* ============ 内嵌 AI 助手聊天 ============ */
.chat-scroll {
  flex: 1;
  min-height: 0;
}

.chat-inner {
  padding: 20rpx 24rpx;
}

.msg-row {
  display: flex;
  align-items: flex-start;
  gap: 16rpx;
  margin-bottom: 26rpx;
}

.msg-row.right {
  flex-direction: row-reverse;
}

.avatar {
  width: 64rpx;
  height: 64rpx;
  border-radius: 18rpx;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 800;
  font-size: 30rpx;
}

.avatar.bot {
  background: linear-gradient(135deg, #52B788, #2D6A4F);
  color: #FFFFFF;
}

.bubble {
  max-width: 74%;
  padding: 20rpx 24rpx;
  font-size: 28rpx;
  line-height: 1.55;
  border-radius: 24rpx;
  white-space: pre-wrap;
  word-break: break-word;
}

.bubble.bot {
  background: #FFFFFF;
  color: #1C2B20;
  border-top-left-radius: 8rpx;
  box-shadow: 0 4rpx 18rpx rgba(28, 43, 32, 0.06);
}

.bubble.user {
  background: linear-gradient(135deg, #52B788, #40916C);
  color: #FFFFFF;
  border-top-right-radius: 8rpx;
}

.suggestions {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  margin: 8rpx 0 26rpx 80rpx;
}

.sug-chip {
  padding: 16rpx 26rpx;
  background: #FFFFFF;
  border: 2rpx solid #D8F3DC;
  color: #2D6A4F;
  font-size: 26rpx;
  border-radius: 36rpx;
  box-shadow: 0 4rpx 14rpx rgba(28, 43, 32, 0.05);
}

.sug-chip:active {
  background: #D8F3DC;
}

.typing {
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.typing .dot {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
  background: #B7C9BD;
  animation: blink 1.2s infinite ease-in-out;
}

.typing .dot:nth-child(2) {
  animation-delay: 0.2s;
}

.typing .dot:nth-child(3) {
  animation-delay: 0.4s;
}

@keyframes blink {
  0%, 80%, 100% {
    opacity: 0.3;
  }
  40% {
    opacity: 1;
  }
}

.bottom-anchor {
  height: 2rpx;
}

.chat-input-bar {
  flex-shrink: 0;
  display: flex;
  align-items: center;
  gap: 16rpx;
  padding: 16rpx 24rpx;
  background: #FFFFFF;
  box-shadow: 0 -4rpx 20rpx rgba(28, 43, 32, 0.06);
}

.chat-input {
  flex: 1;
  height: 76rpx;
  padding: 0 26rpx;
  background: #F4FAF6;
  border-radius: 38rpx;
  font-size: 28rpx;
}

.chat-send {
  flex-shrink: 0;
  height: 76rpx;
  line-height: 76rpx;
  padding: 0 36rpx;
  background: #52B788;
  color: #FFFFFF;
  font-size: 28rpx;
  font-weight: 700;
  border-radius: 38rpx;
}

.chat-send.disabled {
  background: #CFD8DC;
  color: #FFFFFF;
}
